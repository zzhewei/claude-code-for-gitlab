# =================================
# Claude GitLab App Configuration
# =================================

# Application Settings
NODE_ENV=production
PORT=3000
LOG_LEVEL=info
TZ=UTC

# App URLs
APP_URL=https://your-domain.com
GITLAB_URL=https://gitlab.com

# GitLab OAuth Application (Create at https://gitlab.com/-/profile/applications)
GITLAB_APP_ID=your_gitlab_application_id
GITLAB_APP_SECRET=your_gitlab_application_secret
GITLAB_REDIRECT_URI=https://your-domain.com/auth/callback
GITLAB_SCOPE=api read_user read_repository

# Claude API Configuration (Choose ONE)
# Option 1: Direct Anthropic API (recommended)
ANTHROPIC_API_KEY=your_anthropic_api_key

# Option 2: Claude Code OAuth Token (if using claude.ai/code)
# CLAUDE_CODE_OAUTH_TOKEN=your_claude_code_oauth_token

# Claude Model Selection
CLAUDE_MODEL=claude-3-sonnet-20241022

# Security (Generate with: openssl rand -base64 32)
SESSION_SECRET=your_32_character_random_session_secret
ENCRYPTION_KEY=your_32_character_random_encryption_key

# Optional: Cloudflare Tunnel (for public access without port forwarding)
# CLOUDFLARE_TUNNEL_TOKEN=your_cloudflare_tunnel_token
# CLOUDFLARE_TUNNEL_NAME=claude-gitlab-app

# Optional: Redis (for session storage and rate limiting)
REDIS_PASSWORD=changeme
REDIS_URL=redis://redis:6379

# Optional: Security
ALLOWED_ORIGINS=https://your-domain.com,https://gitlab.com
TRUST_PROXY=false
SECURE_HEADERS=true

# Optional: Docker Compose Profiles
# Uncomment to enable services:
# COMPOSE_PROFILES=redis,tunnel    # Enable Redis and Cloudflare Tunnel
# COMPOSE_PROFILES=nginx,redis     # Enable Nginx and Redis
# COMPOSE_PROFILES=full            # Enable all services

# Optional: Monitoring
PROMETHEUS_PORT=9090
GRAFANA_PORT=3001
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin

# Optional: Nginx (if not using Cloudflare Tunnel)
HTTP_PORT=80
HTTPS_PORT=443